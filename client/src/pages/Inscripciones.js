import React from 'react';

const Inscripciones = () => {
  return (
    <div>
      <h2>Inscripciones</h2>
      {/* Aquí irá el BREAD (sin edición), paginación, validaciones y diplomas */}
      <p>Listado, paginación y acciones de inscripciones.</p>
    </div>
  );
};

export default Inscripciones;
